package oops.pen;

/**
 * Person interface is for have notify()
 */
public interface Person {
	// notify() method takes one parameter String type and it's nothing returns
	public void notify(String s);
}
